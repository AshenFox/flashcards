FROM node:20.18.1

WORKDIR /app

COPY package*.json ./

COPY . .

RUN npm install

# Webpack build; increase Node heap for Next.js production build in constrained environments
ENV NODE_OPTIONS="--max-old-space-size=4096"
RUN npm run build

EXPOSE 4000

CMD ["npm", "run", "start"]

